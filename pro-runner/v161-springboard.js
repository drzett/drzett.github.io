const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>[...r.querySelectorAll(s)];
const DB='pro-runner-v1',PROJECTS='projects';
const LAYOUT_KEY='pro-runner-home-layout-v2';
const RUNNER_SETTINGS_KEY='pro-runner-runner-app-v15';
const GRID_COLUMNS=4;

const style=document.createElement('style');style.textContent=`
#proRunnerDragPortal{position:fixed;inset:0;z-index:10000;pointer-events:none;overflow:visible;contain:none}
.pro-drag-ghost{position:fixed;left:0;top:0;pointer-events:none;transform-origin:50% 50%;will-change:transform;filter:drop-shadow(0 16px 28px rgba(0,0,0,.34));z-index:10001}
.pro-drag-ghost>.home-app-icon{width:100%!important;height:100%!important;max-width:none!important;max-height:none!important;margin:0!important;animation:none!important;transform:none!important}
.pro-drag-ghost.home-widget{margin:0!important;animation:none!important;transform:none!important}
.portal-drag-source>.home-app-icon{opacity:.18!important}.portal-drag-source>.home-app-label{opacity:.22!important}.portal-drag-source.home-widget{opacity:.18!important}
.springboard-editing .home-content{overflow:visible!important;contain:layout!important}
.pro-portal-grid-marker{z-index:0;pointer-events:none;border-radius:24%;background:rgba(255,255,255,.085);box-shadow:inset 0 0 0 1.5px rgba(255,255,255,.34),0 0 22px rgba(255,255,255,.06);align-self:stretch;justify-self:stretch;min-width:0;min-height:0;animation:proSnapPulse .14s ease-out both}
.pro-portal-grid-marker.blocked{background:rgba(255,95,95,.08);box-shadow:inset 0 0 0 1.5px rgba(255,120,120,.48)}
.home-dock{position:absolute}
.home-dock.portal-drop-ready{box-shadow:inset 0 0 0 2px rgba(255,255,255,.46),0 8px 26px rgba(0,0,0,.16)!important}
.home-dock.portal-drop-blocked{box-shadow:inset 0 0 0 2px rgba(255,110,110,.72),0 8px 26px rgba(0,0,0,.16)!important}
.pro-dock-slot-marker{position:absolute;top:9px;bottom:9px;pointer-events:none;border-radius:25%;background:rgba(255,255,255,.085);box-shadow:inset 0 0 0 1.5px rgba(255,255,255,.32);z-index:0;animation:proSnapPulse .14s ease-out both}
.pro-dock-slot-marker.blocked{background:rgba(255,95,95,.08);box-shadow:inset 0 0 0 1.5px rgba(255,120,120,.48)}
@keyframes proSnapPulse{from{opacity:.35;scale:.94}to{opacity:1;scale:1}}
`;
document.head.append(style);

function toast(message,duration=2200){const host=$('#toastHost');if(!host)return;const item=document.createElement('div');item.className='toast';item.textContent=message;host.append(item);setTimeout(()=>item.remove(),duration)}
function openDB(){return new Promise((resolve,reject)=>{const request=indexedDB.open(DB,1);request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error||new Error('Unable to open local storage.'))})}
async function getAllProjects(){const db=await openDB();return new Promise((resolve,reject)=>{const tx=db.transaction(PROJECTS,'readonly');const request=tx.objectStore(PROJECTS).getAll();request.onsuccess=()=>resolve(request.result||[]);request.onerror=()=>reject(request.error||new Error('Unable to read projects.'));tx.oncomplete=()=>db.close();tx.onabort=()=>db.close()})}
async function getProject(id){const db=await openDB();return new Promise((resolve,reject)=>{const tx=db.transaction(PROJECTS,'readonly');const request=tx.objectStore(PROJECTS).get(id);request.onsuccess=()=>resolve(request.result||null);request.onerror=()=>reject(request.error||new Error('Unable to read project.'));tx.oncomplete=()=>db.close();tx.onabort=()=>db.close()})}
async function putProject(project){const db=await openDB();return new Promise((resolve,reject)=>{const tx=db.transaction(PROJECTS,'readwrite');tx.objectStore(PROJECTS).put(project);tx.oncomplete=()=>{db.close();resolve()};tx.onerror=()=>{db.close();reject(tx.error||new Error('Unable to save project.'))};tx.onabort=()=>{db.close();reject(tx.error||new Error('Unable to save project.'))}})}
function readRunnerSettings(){try{return{dock:false,...JSON.parse(localStorage.getItem(RUNNER_SETTINGS_KEY)||'{}')}}catch{return{dock:false}}}
function writeRunnerSettings(value){localStorage.setItem(RUNNER_SETTINGS_KEY,JSON.stringify(value))}
function readLayout(){try{const parsed=JSON.parse(localStorage.getItem(LAYOUT_KEY)||'{}');if(parsed?.version===2&&parsed.positions&&typeof parsed.positions==='object')return parsed}catch{}return{version:2,positions:{}}}
function saveLayout(layout){try{localStorage.setItem(LAYOUT_KEY,JSON.stringify(layout))}catch{}}
function runnerNode(){return $$('#homeScreen .home-app').find(n=>n.dataset.runnerApp==='1'||(!n.dataset.id&&$('.home-app-label',n)?.textContent?.trim()==='Pro Runner'))||null}
function itemKey(item){if(item.classList.contains('clock-widget'))return'widget:clock';if(item.classList.contains('calendar-widget'))return'widget:calendar';if(item.classList.contains('home-app')){if(item.dataset.id)return`project:${item.dataset.id}`;if(item.dataset.runnerApp==='1'||$('.home-app-label',item)?.textContent?.trim()==='Pro Runner')return'runner'}return null}
function itemSize(item){return item.classList.contains('home-widget')?{w:2,h:2}:{w:1,h:1}}
function isEditing(){return $('#homeScreen')?.classList.contains('springboard-editing')||!$('#homeEditDone')?.classList.contains('hidden')}
function cleanTransient(node){if(!node)return;for(const prop of['transform','zIndex'])node.style.removeProperty(prop);node.classList.remove('spring-dragging','spring-overflow','portal-drag-source')}
function clearGridPlacement(node){if(!node)return;cleanTransient(node);for(const prop of['gridColumn','gridRow'])node.style.removeProperty(prop);for(const key of['springCol','springRow','springKey'])delete node.dataset[key]}
function applyGridPosition(node,pos,size){cleanTransient(node);node.style.gridColumn=`${pos.col} / span ${size.w}`;node.style.gridRow=`${pos.row} / span ${size.h}`;node.dataset.springCol=String(pos.col);node.dataset.springRow=String(pos.row);const key=itemKey(node);if(key)node.dataset.springKey=key}

const home=$('#homeScreen'),homeContent=home?.querySelector('.home-content'),dock=$('#homeDock'),grid=$('#homeAppGrid');
let portal=$('#proRunnerDragPortal');if(!portal){portal=document.createElement('div');portal.id='proRunnerDragPortal';document.body.append(portal)}
let activeProjectId=null,drag=null,gridMarker=null,dockMarker=null,suppressClickUntil=0,syncQueued=false,syncing=false;
function rememberProject(event){const node=event.target.closest?.('.project-card[data-id],#homeScreen .home-app[data-id]');if(node?.dataset.id)activeProjectId=node.dataset.id}
document.addEventListener('pointerdown',rememberProject,true);document.addEventListener('click',rememberProject,true);

function projectSort(a,b){return(a.homeOrder||a.importedAt||a.createdAt||0)-(b.homeOrder||b.importedAt||b.createdAt||0)}
function measureGrid(){const rect=homeContent.getBoundingClientRect();const cs=getComputedStyle(homeContent);const colGap=parseFloat(cs.columnGap)||0;const rowGap=parseFloat(cs.rowGap)||colGap;const cell=(rect.width-colGap*(GRID_COLUMNS-1))/GRID_COLUMNS;const rows=Math.max(2,Math.floor((rect.height+rowGap)/Math.max(1,cell+rowGap)));return{rect,colGap,rowGap,cell,rows}}
function validPosition(pos,size,m){return Number.isInteger(pos?.col)&&Number.isInteger(pos?.row)&&pos.col>=1&&pos.row>=1&&pos.col+size.w-1<=GRID_COLUMNS&&pos.row+size.h-1<=m.rows}
function cellsFor(pos,size){const out=[];for(let r=pos.row;r<pos.row+size.h;r++)for(let c=pos.col;c<pos.col+size.w;c++)out.push(`${c}:${r}`);return out}
function currentGridItems(){return[...($('#widgetArea')?.querySelectorAll('.home-widget')||[]),...($('#homeAppGrid')?.querySelectorAll('.home-app')||[])]}
function findFirstFree(occupied,size,m){for(let row=1;row<=m.rows-size.h+1;row++)for(let col=1;col<=GRID_COLUMNS-size.w+1;col++){const pos={col,row};if(cellsFor(pos,size).every(cell=>!occupied.has(cell)))return pos}return null}
function normalizeGrid(){if(!homeContent)return;const layout=readLayout(),m=measureGrid(),items=currentGridItems(),occupied=new Map(),accepted=new Set();for(const item of items){const key=itemKey(item);if(!key)continue;const size=itemSize(item),pos=layout.positions[key];if(validPosition(pos,size,m)&&cellsFor(pos,size).every(cell=>!occupied.has(cell))){for(const cell of cellsFor(pos,size))occupied.set(cell,key);accepted.add(key);applyGridPosition(item,pos,size)}}for(const item of items){const key=itemKey(item);if(!key||accepted.has(key))continue;const size=itemSize(item),pos=findFirstFree(occupied,size,m);if(pos){layout.positions[key]=pos;for(const cell of cellsFor(pos,size))occupied.set(cell,key);applyGridPosition(item,pos,size)}else item.classList.add('spring-overflow')}saveLayout(layout)}
function occupancy(excludeKey=null){const layout=readLayout(),map=new Map(),entries=new Map(),m=measureGrid();for(const item of currentGridItems()){const key=itemKey(item);if(!key||key===excludeKey)continue;const size=itemSize(item),pos=layout.positions[key];if(!validPosition(pos,size,m))continue;entries.set(key,{item,size,pos});for(const cell of cellsFor(pos,size))map.set(cell,key)}return{layout,map,entries,m}}
function gridDropInfo(item,key,pos){const {layout,map,entries,m}=occupancy(key),size=itemSize(item);if(!validPosition(pos,size,m))return{allowed:false,layout,entries,size,m,conflicts:[]};const conflicts=[...new Set(cellsFor(pos,size).map(c=>map.get(c)).filter(Boolean))];if(!conflicts.length)return{allowed:true,layout,entries,size,m,conflicts};if(size.w===1&&size.h===1&&conflicts.length===1){const other=entries.get(conflicts[0]);if(other?.size.w===1&&other?.size.h===1)return{allowed:true,layout,entries,size,m,conflicts}}return{allowed:false,layout,entries,size,m,conflicts}}
function targetFromPoint(event,item){const m=measureGrid(),size=itemSize(item),pitchX=m.cell+m.colGap,pitchY=m.cell+m.rowGap;const left=event.clientX-drag.offsetX-m.rect.left,top=event.clientY-drag.offsetY-m.rect.top;let col=Math.round(left/pitchX)+1,row=Math.round(top/pitchY)+1;col=Math.max(1,Math.min(GRID_COLUMNS-size.w+1,col));row=Math.max(1,Math.min(m.rows-size.h+1,row));return{col,row,m,size}}

function dockEntriesFrom(projects,runnerSettings){const entries=projects.filter(p=>p.dock).map(p=>({kind:'project',id:p.id,order:Number.isFinite(p.dockOrder)?p.dockOrder:1000+(p.homeOrder||p.importedAt||p.createdAt||0),project:p}));if(runnerSettings.dock)entries.push({kind:'runner',id:'runner',order:Number.isFinite(runnerSettings.dockOrder)?runnerSettings.dockOrder:999999});return entries.sort((a,b)=>a.order-b.order||String(a.id).localeCompare(String(b.id))).slice(0,4)}
async function persistDockOrder(entries,projects){const projectMap=new Map(projects.map(p=>[p.id,p]));for(let i=0;i<entries.length;i++){const entry=entries[i];if(entry.kind==='project'){const p=projectMap.get(entry.id);if(p&&(p.dockOrder!==i||!p.dock)){p.dock=true;p.dockOrder=i;p.updatedAt=Date.now();await putProject(p)}}else{const s=readRunnerSettings();if(!s.dock||s.dockOrder!==i){s.dock=true;s.dockOrder=i;writeRunnerSettings(s)}}}}
async function setDockMembership(kind,id,wants,targetIndex=null,{quiet=false}={}){const projects=(await getAllProjects()).sort(projectSort),runnerSettings=readRunnerSettings();let entries=dockEntriesFrom(projects,runnerSettings).filter(e=>!(e.kind===kind&&e.id===id));if(wants){if(entries.length>=4){if(!quiet)toast('The dock can contain up to four items.');return false}const index=Number.isInteger(targetIndex)?Math.max(0,Math.min(entries.length,targetIndex)):entries.length;const project=kind==='project'?projects.find(p=>p.id===id):null;entries.splice(index,0,{kind,id,order:index,project})}if(kind==='project'){const p=projects.find(row=>row.id===id);if(!p)return false;p.dock=Boolean(wants);if(!wants)delete p.dockOrder;p.updatedAt=Date.now();await putProject(p)}else{const s=readRunnerSettings();s.dock=Boolean(wants);if(!wants)delete s.dockOrder;writeRunnerSettings(s)}if(wants)await persistDockOrder(entries,projects);queueSync();return true}
async function requestProjectDock(projectId,wants,options={}){return setDockMembership('project',projectId,wants,options.targetIndex??null,options)}
async function requestRunnerDock(wants,options={}){return setDockMembership('runner','runner',wants,options.targetIndex??null,options)}

async function syncDockAndGrid(){if(syncing||!home||!dock||!grid)return;syncing=true;try{const projects=(await getAllProjects()).sort(projectSort),runnerSettings=readRunnerSettings(),entries=dockEntriesFrom(projects,runnerSettings);const desired=new Set(entries.map(e=>`${e.kind}:${e.id}`));for(const p of projects){const node=home.querySelector(`.home-app[data-id="${CSS.escape(p.id)}"]`);if(!node)continue;const key=`project:${p.id}`;if(desired.has(key)){if(node.parentElement!==dock){clearGridPlacement(node);dock.append(node)}}else if(node.parentElement===dock){clearGridPlacement(node);grid.append(node)}}const runner=runnerNode();if(runner){runner.dataset.runnerApp='1';if(desired.has('runner:runner')){if(runner.parentElement!==dock){clearGridPlacement(runner);dock.append(runner)}}else if(runner.parentElement===dock){clearGridPlacement(runner);grid.prepend(runner)}}for(let i=0;i<entries.length;i++){const entry=entries[i];const node=entry.kind==='runner'?runner:home.querySelector(`.home-app[data-id="${CSS.escape(entry.id)}"]`);if(!node)continue;if(node.parentElement!==dock)dock.append(node);clearTransient(node);node.style.gridColumn=String(i+1);node.style.gridRow='1';node.style.alignSelf='stretch';node.style.justifySelf='stretch'}for(const node of $$('#homeDock .home-app')){const inEntries=entries.some(e=>(e.kind==='runner'&&node===runner)||(e.kind==='project'&&node.dataset.id===e.id));if(!inEntries){clearGridPlacement(node);grid.append(node)}}dock.style.opacity=entries.length?'1':'.45';normalizeGrid();syncDockToggles(projects,runnerSettings)}finally{syncing=false}}
function queueSync(){if(syncQueued)return;syncQueued=true;requestAnimationFrame(()=>{syncQueued=false;void syncDockAndGrid()})}
function syncDockToggles(projects=null,runnerSettings=null){const projectToggle=$('#projectDockToggle');if(projectToggle&&activeProjectId){const p=projects?.find(x=>x.id===activeProjectId);if(p)projectToggle.checked=Boolean(p.dock);else void getProject(activeProjectId).then(row=>{if(row)projectToggle.checked=Boolean(row.dock)}).catch(()=>{})}const runnerToggle=$('#runnerDockToggle');if(runnerToggle)runnerToggle.checked=Boolean((runnerSettings||readRunnerSettings()).dock)}

document.addEventListener('change',event=>{const toggle=event.target;if(toggle?.id!=='projectDockToggle'&&toggle?.id!=='runnerDockToggle')return;event.preventDefault();event.stopImmediatePropagation();const wants=toggle.checked;void(async()=>{try{const ok=toggle.id==='runnerDockToggle'?await requestRunnerDock(wants):activeProjectId?await requestProjectDock(activeProjectId,wants):false;if(!ok)toggle.checked=!wants;else{toggle.checked=wants;await syncDockAndGrid()}}catch(error){toggle.checked=!wants;toast(error?.message||'Unable to update the dock.')}})()},true);

function showGridMarker(pos,size,allowed){if(!gridMarker){gridMarker=document.createElement('div');gridMarker.className='pro-portal-grid-marker';homeContent.append(gridMarker)}gridMarker.classList.toggle('blocked',!allowed);gridMarker.style.gridColumn=`${pos.col} / span ${size.w}`;gridMarker.style.gridRow=`${pos.row} / span ${size.h}`}
function dockMetrics(){const rect=dock.getBoundingClientRect(),cs=getComputedStyle(dock),pl=parseFloat(cs.paddingLeft)||0,pr=parseFloat(cs.paddingRight)||0,gap=parseFloat(cs.columnGap)||parseFloat(cs.gap)||0,inner=Math.max(1,rect.width-pl-pr),cell=(inner-gap*3)/4;return{rect,pl,pr,gap,cell}}
function dockTargetIndex(x){const m=dockMetrics(),pitch=m.cell+m.gap,raw=Math.round((x-(m.rect.left+m.pl)-m.cell/2)/pitch);return Math.max(0,Math.min(3,raw))}
function showDockMarker(index,allowed){if(!dockMarker){dockMarker=document.createElement('div');dockMarker.className='pro-dock-slot-marker';dock.append(dockMarker)}const m=dockMetrics(),left=m.pl+index*(m.cell+m.gap);dockMarker.classList.toggle('blocked',!allowed);dockMarker.style.left=`${left}px`;dockMarker.style.width=`${m.cell}px`}
function clearTargets(){gridMarker?.remove();gridMarker=null;dockMarker?.remove();dockMarker=null;dock?.classList.remove('portal-drop-ready','portal-drop-blocked')}
function pointInRect(x,y,rect,pad=0){return x>=rect.left-pad&&x<=rect.right+pad&&y>=rect.top-pad&&y<=rect.bottom+pad}
function currentDockCountExcluding(item){return $$('#homeDock .home-app').filter(n=>n!==item).length}
function updateTarget(event){clearTargets();if(!drag)return;const item=drag.item;if(item.classList.contains('home-app')&&pointInRect(event.clientX,event.clientY,dock.getBoundingClientRect(),14)){const index=dockTargetIndex(event.clientX),allowed=currentDockCountExcluding(item)<4;drag.zone='dock';drag.dockTarget=index;drag.gridTarget=null;drag.allowed=allowed;dock.classList.add(allowed?'portal-drop-ready':'portal-drop-blocked');showDockMarker(index,allowed);return}drag.zone='grid';drag.dockTarget=null;const target=targetFromPoint(event,item),info=gridDropInfo(item,drag.key,{col:target.col,row:target.row});drag.gridTarget={col:target.col,row:target.row};drag.allowed=info.allowed;showGridMarker(drag.gridTarget,info.size,info.allowed)}
function buildGhost(item,visualRect){const ghost=document.createElement('div');if(item.classList.contains('home-widget')){ghost.className=`pro-drag-ghost home-widget ${item.className.replace(/\bspring-dragging\b/g,'')}`;const clone=item.cloneNode(true);clone.querySelectorAll('[id]').forEach(n=>n.removeAttribute('id'));ghost.replaceChildren(...clone.childNodes)}else{ghost.className='pro-drag-ghost';const icon=item.querySelector('.home-app-icon')?.cloneNode(true);if(icon){icon.querySelectorAll?.('[id]').forEach(n=>n.removeAttribute('id'));ghost.append(icon)}}ghost.style.width=`${visualRect.width}px`;ghost.style.height=`${visualRect.height}px`;portal.append(ghost);return ghost}
function positionGhost(event){if(!drag?.ghost)return;const x=event.clientX-drag.offsetX,y=event.clientY-drag.offsetY;drag.ghost.style.transform=`translate3d(${x}px,${y}px,0) scale(1.055)`}
function beginDrag(event){if(!drag||drag.started)return;drag.started=true;drag.item.classList.add('portal-drag-source');drag.ghost=buildGhost(drag.item,drag.visualRect);try{home.setPointerCapture(event.pointerId)}catch{}positionGhost(event);updateTarget(event)}
function commitGridPosition(item,key,pos,sourceWasGrid){const info=gridDropInfo(item,key,pos);if(!info.allowed)return false;const layout=info.layout,oldPos=layout.positions[key]?{...layout.positions[key]}:null;if(info.conflicts.length){const otherKey=info.conflicts[0],other=info.entries.get(otherKey);if(!other)return false;if(sourceWasGrid&&oldPos&&validPosition(oldPos,other.size,info.m)){layout.positions[otherKey]=oldPos;applyGridPosition(other.item,oldPos,other.size)}else{const occupied=new Map();for(const [entryKey,entry] of info.entries){if(entryKey===otherKey)continue;for(const cell of cellsFor(entry.pos,entry.size))occupied.set(cell,entryKey)}for(const cell of cellsFor(pos,info.size))occupied.set(cell,key);const free=findFirstFree(occupied,other.size,info.m);if(!free)return false;layout.positions[otherKey]=free;applyGridPosition(other.item,free,other.size)}}layout.positions[key]={...pos};saveLayout(layout);applyGridPosition(item,pos,info.size);return true}
async function commitDrop(active){if(!active.allowed)return false;const item=active.item,key=active.key;if(active.zone==='dock'){if(!item.classList.contains('home-app'))return false;const ok=item.dataset.id?await requestProjectDock(item.dataset.id,true,{targetIndex:active.dockTarget,quiet:true}):await requestRunnerDock(true,{targetIndex:active.dockTarget,quiet:true});if(ok)await syncDockAndGrid();return ok}if(active.zone==='grid'&&active.gridTarget){if(!commitGridPosition(item,key,active.gridTarget,active.source==='grid'))return false;if(item.classList.contains('home-app')){const ok=item.dataset.id?await requestProjectDock(item.dataset.id,false,{quiet:true}):await requestRunnerDock(false,{quiet:true});if(!ok)return false}await syncDockAndGrid();normalizeGrid();return true}return false}

if(home){home.addEventListener('pointerdown',event=>{if(!isEditing()||event.button>0||event.target.closest('.app-delete-badge,button,input,select'))return;const item=event.target.closest('.home-app,.home-widget');if(!item||!home.contains(item))return;event.stopPropagation();const visual=item.classList.contains('home-app')?(item.querySelector('.home-app-icon')||item):item,rect=visual.getBoundingClientRect();drag={item,key:itemKey(item),pointerId:event.pointerId,startX:event.clientX,startY:event.clientY,offsetX:event.clientX-rect.left,offsetY:event.clientY-rect.top,visualRect:rect,source:item.parentElement===dock?'dock':'grid',started:false,ghost:null,zone:null,gridTarget:null,dockTarget:null,allowed:false};if(!drag.key)drag=null},true);
home.addEventListener('pointermove',event=>{if(!drag||drag.pointerId!==event.pointerId)return;const distance=Math.hypot(event.clientX-drag.startX,event.clientY-drag.startY);if(!drag.started&&distance<6)return;event.preventDefault();event.stopPropagation();if(!drag.started)beginDrag(event);positionGhost(event);updateTarget(event)},{capture:true,passive:false});
const finish=event=>{if(!drag||drag.pointerId!==event.pointerId)return;const active=drag;drag=null;if(!active.started)return;event.preventDefault();event.stopPropagation();suppressClickUntil=performance.now()+700;void(async()=>{let ok=false;try{ok=await commitDrop(active)}catch(error){toast(error?.message||'Unable to move this item.')}active.item.classList.remove('portal-drag-source');active.ghost?.remove();clearTargets();try{if(home.hasPointerCapture?.(event.pointerId))home.releasePointerCapture(event.pointerId)}catch{}if(!ok){active.item.animate([{scale:'1.04'},{scale:'1'}],{duration:160,easing:'ease-out'});queueSync()}})()};
home.addEventListener('pointerup',finish,true);home.addEventListener('pointercancel',finish,true);home.addEventListener('click',event=>{if(performance.now()<suppressClickUntil){event.preventDefault();event.stopImmediatePropagation()}},true)}

const observer=new MutationObserver(()=>queueSync());if(home)observer.observe(home,{subtree:true,childList:true});window.addEventListener('resize',queueSync,{passive:true});window.visualViewport?.addEventListener('resize',queueSync,{passive:true});const dialogObserver=new MutationObserver(()=>syncDockToggles());dialogObserver.observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['open']});
await syncDockAndGrid();
window.ProRunnerDockV161={requestProjectDock,requestRunnerDock,sync:syncDockAndGrid};
